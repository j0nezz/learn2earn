import styled from 'styled-components';
import {SPACING} from '../../theme/theme';

export const PageContainer = styled.div`
  width: 100%;
  max-width: 1440px;
  padding: 0 2rem;
  margin: auto auto ${SPACING * 2}px;
`;
